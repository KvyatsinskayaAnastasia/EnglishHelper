spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/english_test
    username: postgres
    password: postgres
  liquibase:
    change-log: classpath:/db/changelog/db.changelog-master.yaml

logging:
  level:
    org.hibernate: DEBUG
    org.springframework: DEBUG