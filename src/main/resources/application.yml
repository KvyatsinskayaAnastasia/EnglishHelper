spring:
  liquibase:
    change-log: classpath:/db/changelog/db.changelog-master.yaml
  datasource:
    url: jdbc:postgresql://localhost:5432/english
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  application:
   name: EnglishHelperBot
  data:
    redis:
      host: localhost
      port: 6379
      password: Cat77Dog99
      database: 0
      timeout: 3000ms
bot:
  name:
  token:
#debug: true
#logging:
#  level:
#    org:
#      springframework:
#        web: DEBUG
#    root: DEBUG